# You can add comments in the config like this (prefix #)
# Always put the comment on its own line, otherwise it will get mixed up with the JSON!
# Channel types: default(the first public channel the user goes to after join), private, global
# Add as many channels as you want!
# You can use color codes in the Motd!

{
  "password": "",
  "port": 38000,
  "rsa_length": 2048,
  "slots": 50,
  "channels": [
      {
        "name":"global",
        "type":"default"
      },
      {
        "name":"channel2",
        "type":"private",
        "password": "secret"
      },
      {
        "name":"channel3",
        "type":"public"
      }
    ],
  "forbidden_ips": [
      "example168.123.1.1"
    ],
  "forbidden_usernames": [
      "admin",
      "administrator",
      "server"
    ],
  "forbidden_commands": [
      "/example",
      "nodashworkstoo"
    ],
  "motd": "~1Hello, welcome to our chat server! :) Type /help to check what you can do here."
}